<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.payments.platform.service.mapper.AdminAuthMapper">
  <select id="findActiveUser" resultType="com.example.payments.platform.service.mapper.AdminAuthMapper$UserRow">
    SELECT id, username, password_hash, display_name
    FROM admin_user
    WHERE username = #{username}
    AND status = 'ACTIVE'
  </select>
  <select id="findActiveUserWithoutPassword" resultType="com.example.payments.platform.service.mapper.AdminAuthMapper$UserRow">
    SELECT id, username, '' AS password_hash, display_name
    FROM admin_user
    WHERE username = #{username}
    AND status = 'ACTIVE'
  </select>
  <select id="findRoles" resultType="string">
    SELECT r.role_code
    FROM admin_role r
    JOIN admin_user_role ur ON ur.role_id = r.id
    WHERE ur.user_id = #{userId}
    ORDER BY r.role_code
  </select>
</mapper>
