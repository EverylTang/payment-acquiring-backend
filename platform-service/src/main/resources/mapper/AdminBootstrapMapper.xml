<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.payments.platform.service.mapper.AdminBootstrapMapper">
  <insert id="insertAdminUser">
    INSERT INTO admin_user (username, password_hash, display_name, status, created_at, updated_at)
    VALUES (#{username}, #{passwordHash}, #{displayName}, 'ACTIVE', #{now}, #{now})
  </insert>
  <select id="selectUserId" resultType="long">SELECT id FROM admin_user WHERE username = #{username}</select>
  <select id="selectAdminRoleId" resultType="long">SELECT id FROM admin_role WHERE role_code = 'ADMIN'</select>
  <insert id="insertUserRole">INSERT INTO admin_user_role (user_id, role_id) VALUES (#{userId}, #{roleId})</insert>
  <insert id="insertSystemMenu">
    INSERT IGNORE INTO admin_menu (parent_id, menu_code, menu_name, menu_type, route_path, component_key, icon, sort_order, visible, status, created_at, updated_at)
    VALUES (0, #{menuCode}, '菜单管理', 'PAGE', '/menus', 'menus', 'Settings2', 93, TRUE, 'ACTIVE', #{now}, #{now})
  </insert>
  <insert id="assignSystemMenuToAdmin">
    INSERT IGNORE INTO admin_role_menu (role_id, menu_id)
    SELECT r.id, m.id FROM admin_role r JOIN admin_menu m ON m.menu_code = #{menuCode} WHERE r.role_code = 'ADMIN'
  </insert>
  <insert id="assignAllActivePermissionsToAdmin">
    INSERT IGNORE INTO admin_role_permission (role_id, permission_id)
    SELECT r.id, p.id FROM admin_role r CROSS JOIN admin_permission p WHERE r.role_code = 'ADMIN' AND p.status = 'ACTIVE'
  </insert>
  <select id="countTables" resultType="long">
    SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = DATABASE() AND table_name = #{tableName}
  </select>
  <select id="countAdminUsers" resultType="long">SELECT COUNT(*) FROM admin_user</select>
</mapper>
