<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.payments.platform.service.mapper.MerchantCredentialMapper">
  <select id="selectByMerchantId" resultType="com.example.payments.platform.service.model.MerchantCredentialModel">
    SELECT merchant_id, api_key, api_secret_hash, created_at, updated_at
    FROM merchant_credential
    WHERE merchant_id = #{merchantId}
  </select>
  <insert id="upsert">
    INSERT INTO merchant_credential (merchant_id, api_key, api_secret_hash, created_at, updated_at)
    VALUES (#{merchantId}, #{apiKey}, #{apiSecretHash}, #{now}, #{now})
    ON DUPLICATE KEY UPDATE
      api_key = VALUES(api_key),
      api_secret_hash = VALUES(api_secret_hash),
      updated_at = VALUES(updated_at)
  </insert>
</mapper>
