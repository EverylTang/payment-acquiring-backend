<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.payments.platform.service.mapper.OperationAuditQueryMapper">
  <sql id="filters">
    WHERE (#{resourceType} IS NULL
    OR resource_type = #{resourceType}) AND (#{operatorId} IS NULL
    OR operator_id = #{operatorId})
  </sql>
  <select id="count" resultType="long">
    SELECT COUNT(*)
    FROM operation_audit
    <include refid="filters" />
  </select>
  <select id="select" resultType="com.example.payments.platform.service.model.OperationAuditModel">
    SELECT audit_id, operator_id, action, resource_type, resource_id, request_id, reason, before_summary, after_summary, created_at
    FROM operation_audit
    <include refid="filters" />
    ORDER BY created_at DESC
    LIMIT #{limit}
    OFFSET #{offset}
  </select>
</mapper>
