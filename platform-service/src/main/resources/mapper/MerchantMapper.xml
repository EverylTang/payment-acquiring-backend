<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.payments.platform.service.mapper.MerchantMapper">
  <resultMap id="MerchantMap" type="com.example.payments.platform.service.model.MerchantModel">
    <id property="merchantId" column="merchant_id" />
    <result property="name" column="name" />
    <result property="status" column="status" />
    <result property="settlementCurrency" column="settlement_currency" />
    <result property="createdAt" column="created_at" />
    <result property="updatedAt" column="updated_at" />
  </resultMap>
  <sql id="scope">
    (#{allScope} = TRUE
    OR m.merchant_id IN (SELECT ums.merchant_id
    FROM admin_user_merchant_scope ums
    JOIN admin_user u ON u.id = ums.user_id
    WHERE u.username = #{username}))
  </sql>
  <select id="countVisible" resultType="long">
    SELECT COUNT(*)
    FROM merchant m
    WHERE
    <include refid="scope" />
  </select>
  <select id="selectVisible" resultMap="MerchantMap">
    SELECT merchant_id,name,status,settlement_currency,created_at,updated_at
    FROM merchant m
    WHERE
    <include refid="scope" />
    ORDER BY created_at DESC
    LIMIT #{limit}
    OFFSET #{offset}
  </select>
  <select id="selectVisibleById" resultMap="MerchantMap">
    SELECT merchant_id,name,status,settlement_currency,created_at,updated_at
    FROM merchant m
    WHERE merchant_id=#{merchantId} AND
    <include refid="scope" />
  </select>
  <insert id="insertMerchant">
    INSERT INTO merchant (merchant_id,name,status,settlement_currency,created_at,updated_at)
    VALUES (#{merchantId},#{name},'ACTIVE',#{currency},#{now},#{now})
  </insert>
  <update id="updateMerchant">
    UPDATE merchant
    SET name=#{name},settlement_currency=#{currency},updated_at=#{now}
    WHERE merchant_id=#{merchantId}
  </update>
  <update id="updateStatus">
    UPDATE merchant
    SET status=#{status},updated_at=#{now}
    WHERE merchant_id=#{merchantId}
  </update>
</mapper>
