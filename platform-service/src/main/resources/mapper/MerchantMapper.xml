<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.payments.platform.service.mapper.MerchantMapper">
  <resultMap id="MerchantMap" type="com.example.payments.platform.service.model.MerchantModel">
    <constructor>
      <idArg column="merchant_id" javaType="java.lang.String" />
      <arg column="name" javaType="java.lang.String" />
      <arg column="status" javaType="java.lang.String" />
      <arg column="created_at" javaType="java.time.Instant" />
      <arg column="updated_at" javaType="java.time.Instant" />
    </constructor>
  </resultMap>
  <sql id="scope">
    (#{allScope} = TRUE
    OR m.merchant_id IN (SELECT ums.merchant_id
    FROM admin_user_merchant_scope ums
    JOIN admin_user u ON u.id = ums.user_id
    WHERE u.username = #{username}))
  </sql>
  <select id="countVisible" resultType="long">
    SELECT COUNT(*)
    FROM merchant m
    WHERE
    <include refid="scope" />
  </select>
  <select id="selectVisible" resultMap="MerchantMap">
    SELECT merchant_id,name,status,created_at,updated_at
    FROM merchant m
    WHERE
    <include refid="scope" />
    ORDER BY created_at DESC
    LIMIT #{limit}
    OFFSET #{offset}
  </select>
  <select id="selectVisibleById" resultMap="MerchantMap">
    SELECT merchant_id,name,status,created_at,updated_at
    FROM merchant m
    WHERE merchant_id=#{merchantId} AND
    <include refid="scope" />
  </select>
  <insert id="insertMerchant">
    INSERT INTO merchant (merchant_id,name,status,created_at,updated_at)
    VALUES (#{merchantId},#{name},'ACTIVE',#{now},#{now})
  </insert>
  <insert id="insertMerchantWithGeneratedId">
    INSERT INTO merchant (merchant_id,name,status,created_at,updated_at)
    VALUES (CONCAT('__pending__', UUID()),#{name},'ACTIVE',#{now},#{now})
  </insert>
  <select id="selectLastInsertId" resultType="long">
    SELECT LAST_INSERT_ID()
  </select>
  <update id="updateMerchantId">
    UPDATE merchant SET merchant_id=#{merchantId} WHERE id=#{id}
  </update>
  <update id="updateMerchant">
    UPDATE merchant
    SET name=#{name},updated_at=#{now}
    WHERE merchant_id=#{merchantId}
  </update>
  <update id="updateStatus">
    UPDATE merchant
    SET status=#{status},updated_at=#{now}
    WHERE merchant_id=#{merchantId}
  </update>
</mapper>
